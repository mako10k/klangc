# COMMENT
main =
 \args ->
   join (fmap printInt (fib 5));

join = \m -> chain m id;
id = \x -> x;
chain = \m -> \f -> join (fmap f m);
fmap = \f -> \m -> chain m \x -> return (f x);

fib = 
  \0 -> 1 |
  \1 -> 1 |
  \Int n -> add (fib (pred n)) (fib (pred (pred n)));